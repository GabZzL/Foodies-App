export default function NotFoundPage() {
    return (
      <main className="not-found">
        <h1>Meals not found</h1>
        <p>Unfortunately, we could not find the meal resources.</p>
      </main>
    );
  }